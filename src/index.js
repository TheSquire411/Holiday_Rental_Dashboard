import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './lodgify-dashboard'; // Assuming the file is named this

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
